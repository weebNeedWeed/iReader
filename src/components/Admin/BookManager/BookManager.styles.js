import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  container: {
    paddingTop: "42px",
    paddingLeft: "30px",
    paddingRight: "30px",
  },
});

export default useStyles;
